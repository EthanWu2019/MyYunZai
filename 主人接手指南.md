# YunZai_APP 主人接手指南

## 项目位置

- **代码仓库**: https://github.com/EthanWu2019/MyYunZai
- **本地代码**: `E:\YunZai_APP\` (游戏本上)
- **mac 副本**: `~/workspace/yunzai-app/` (Mac 上,已 push)

## 当前状态 (2026-09-14 已完成 95%)

✅ 已完成:
- 完整 Tauri 2 项目 (Rust 后端 + React 前端)
- 代码已推到 GitHub (master branch, main 已有 4 commits)
- Rust 1.98.1 已装 (`C:\Users\34018\.cargo\bin\`)
- Node.js 20.11.1 已装
- `cargo check` 通过 (1m55s, 0 错误)
- `vite build` 通过 (产物 278KB JS + 12KB CSS = 94KB gzip)
- 桌面有 `安装VSBuildTools.bat` 和 `打包YunZaiAPP.bat`

❌ 唯一阻塞:
- **VS BuildTools 还没装** — SSH 装失败 (service session 0 限制, vs_setup 假成功), schtasks 也失败

## 主人接手 3 步 (10-15 分钟)

### 第 1 步: 装 VS BuildTools

在游戏本面前, 双击桌面 **`安装VSBuildTools.bat`**:
- 弹 UAC → 点"是"
- 后台静默安装 5-15 分钟 (期间电脑照常用)
- 装完弹窗"Press Enter to close" → 关窗

或者手动 GUI 装: https://aka.ms/vs/17/release/vs_BuildTools.exe
- 选 "使用 C++ 的桌面开发" 工作负载
- 右侧再勾选 "Windows 11 SDK"
- 安装 (10-15 分钟)

### 第 2 步: 验证装好

告诉我 "装好了" 或者我远程 SSH 验证:
```
Test-Path 'C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Tools\MSVC'
```
看到 True 即可。

### 第 3 步: 打包

双击桌面 **`打包YunZaiAPP.bat`**:
- 自动跑 `cargo fetch` + `tauri build`
- 5-15 分钟编译
- 完成后自动弹窗打开产物目录

产物位置:
- `E:\YunZai_APP\src-tauri\target\release\bundle\msi\*.msi` (Windows Installer)
- `E:\YunZai_APP\src-tauri\target\release\bundle\nsis\*.exe` (NSIS 安装器)

## 功能特性 (打包后可用)

- 主窗口 4 个状态卡: Redis / Yunzai / NapCat / QQ (脉冲灯 + PID 显示)
- 一键启动 / 一键关闭 按钮 (保留 Redis)
- 实时日志面板 (tail -f Yunzai + NapCat 日志)
- 嵌 WebUI 标签页 (iframe 嵌 `localhost:6099/webui/`, 直接扫码登录)
- 设置面板: 改路径 + 开机自启 + 关窗最小化
- 系统托盘: 左键显窗口 / 右键菜单 (显示/隐藏/WebUI/退出)
- 单实例: 双开聚焦已有窗口
- 自动持久化配置到 `%APPDATA%/com.ethanwu.yunzai/config.json`

## 重要设计

- **不影响现运行机器人**: App 默认指向 `D:\TRSSYUNZAI\` + `D:\NapCat.Shell\` + nvm 装的 node, 直接调用现有路径, 不修改任何原文件
- **启动顺序铁律**: Redis → Yunzai (等 2536 端口) → NapCat (等 6099 端口)
- **关闭顺序**: 杀 NapCat + QQ + Yunzai, **保留 Redis**
- **扫码**: NapCat 终端自带二维码无效, 必须 WebUI 里扫

## 我 (海绵酱) 的 SSH 远程能力

- 装 Rust ✅
- 装 npm 依赖 ✅
- cargo check / build (除了 final link, 都不需要 VS) ✅
- vite build ✅
- git push GitHub ✅
- 装 VS BuildTools ❌ (SSH service session 限制, 5+ 路线全部失败)
